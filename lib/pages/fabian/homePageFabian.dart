import 'package:flutter/material.dart';

class HomePageFabian extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: new Image.asset('assets/main.png'),
    );
  }
}